# Index

This folder is used to store the fixed property graph index required by DysLexLens.

The fixed index is used during retrieval and question answering. It allows the tool to reuse the same indexed corpus across different runs, which helps keep the evaluation process consistent and reproducible.

## Folder Structure

```text
index/
├── README.md
└── fixed_property_graph_index/
    └── .gitkeep
